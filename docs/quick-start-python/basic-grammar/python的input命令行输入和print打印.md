# python的input命令行输入和print打印

## input

python 使用 input 关键字来接受并等待用户在命令行的输入，返回值来接收用户的输入值

## print

使用 `print` 来打印输出,类似于 `console.log`

想打印什么变量直接 print 打印即可

常用 `f函数`来接收带参字符串类似于JS中的`模板字符串`

```python
name='Bob'
age=19
print(45)
f'{name}，你今年的年龄是{age}'
```
